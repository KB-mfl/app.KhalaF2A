// import http from 'axios'

export default () => new Promise((resolve, reject) => {
  // http.get('auth/profile').then(r => {
  //   window.auth = r.data
  //   resolve();
  // }).catch(e => {
  //   reject();
  // });

  resolve()
});